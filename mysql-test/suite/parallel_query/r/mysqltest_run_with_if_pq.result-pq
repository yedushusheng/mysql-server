CREATE TABLE t1(a INT, b char(2));
INSERT INTO t1 VALUES(1, 'ab'), (2, 'AB'), (3, 'aB'), (4, 'Ab'), (5, 'ac'), (6, 'AC'), (1, 'ab'), (2, 'AB'), (3, 'aB'), (4, 'Ab'), (5, 'ac'), (6, 'AC');
### Sorted and lowercase result
select * from t1;
a	b
1	ab
1	ab
2	ab
2	ab
3	ab
3	ab
4	ab
4	ab
5	ac
5	ac
6	ac
6	ac
### NOT sorted and lowercase result
SELECT /*+ no_parallel */ * FROM t1;
a	b
1	ab
2	AB
3	aB
4	Ab
5	ac
6	AC
1	ab
2	AB
3	aB
4	Ab
5	ac
6	AC
### Disable result log
SELECT * FROM t1 LIMIT 4;
### DISPLAY result log again
SELECT * FROM t1 ORDER BY 1 LIMIT 4;
a	b
1	ab
1	ab
2	AB
2	AB
### Disable warnings
EXPLAIN SELECT * FROM t1 LIMIT 4;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	12	100.00	Parallel scan (4 workers)
### DISPLAY warnings again
EXPLAIN SELECT * FROM t1 LIMIT 4;
id	select_type	table	partitions	type	possible_keys	key	key_len	ref	rows	filtered	Extra
1	SIMPLE	t1	NULL	ALL	NULL	NULL	NULL	NULL	12	100.00	Parallel scan (4 workers)
Warnings:
Note	1003	/* select#1 */ select `test`.`t1`.`a` AS `a`,`test`.`t1`.`b` AS `b` from `test`.`t1` limit 4
Note	1003	Query is executed in a parallel plan;  explain with tree format to see the plan details.
### Replace 2nd column with #
SELECT a, b FROM t1 GROUP BY a;
a	b
1	#
2	#
3	#
4	#
5	#
6	#
### NO replace 2nd column
SELECT /*+ no_parallel */ a, b FROM t1 GROUP BY a;
a	b
1	ab
2	AB
3	aB
4	Ab
5	ac
6	AC
DROP TABLE t1;
CREATE TABLE t2(a double);
INSERT INTO t2 VALUES(100002.244432344), (222201.33443242), (3432233.44453434), (4322344.554443233), (63233.444333233);
### Replace numeric round
SELECT avg(a) FROM t2;
avg(a)
1628003.004435
### NOT replace numeric round
SELECT avg(a) FROM t2;
avg(a)
1628003.0044351139
DROP TABLE t2;
